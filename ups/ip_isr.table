setupRequired(meas_algorithms >= 4.7.3.0)

setupOptional(sdqa >= 4.6.0.0)

envPrepend(DYLD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
envPrepend(LD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
envPrepend(PYTHONPATH, ${PRODUCT_DIR}/python)
