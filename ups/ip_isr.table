setupRequired(afw >= 3.3.4)
setupRequired(meas_algorithms >= 3.0.0)


setupOptional(gcc)
setupOptional(sdqa)
setupOptional(pex_harness)
setupOptional(ctrl_dc3pipe)
setupOptional(isrdata)

envPrepend(DYLD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
envPrepend(LD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
envPrepend(PYTHONPATH, ${PRODUCT_DIR}/python)
